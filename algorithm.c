#include <stdbool.h>

// The idea is to ask if you can put num in another cell in said row/col/box
// if returns true, then can't replace
bool tryPut(int row, int col, int num) {
	bool didPut = true;


	return didPut;
}

bool canPutRow(int row, int num) {
	int normRow = (row/9);
	normRow = normRow*9;

	// check if already in the row


}
bool canPutCol(int row, int col, int num) {

}
bool canPutBox(int row, int col, int num) {

}

void solve(int * puzzle) {
	
}
